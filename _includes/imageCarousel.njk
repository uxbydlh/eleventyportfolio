{%- macro tag(params) -%}
   <span class="tag" style="color: chartreuse;">{{params.text}}</span>
{%- endmacro -%}

{%- macro carousel(params) -%}

   <div class="button-carousel">
      <div class="button-carousel-images">
         {% for image in params.images %}
            <a rel="{{params.carouselId}}" class="{{params.carouselId}} swipebox" href="{{image}}" title="">
               <img src="{{image}}">
            </a>
            <!--<img class="{{params.carouselId}}" src="{{image}}">-->
         {% endfor %}
      </div>
      <div class="row button-carousel-buttons">
         <button class="icon-button" onclick="plusDivs(-1, '{{params.carouselId}}')">
            <i class="ri-arrow-left-s-fill"></i>
        </button>
        <button class="icon-button" onclick="plusDivs(1, '{{params.carouselId}}')">
            <i class="ri-arrow-right-s-fill"></i>
        </button>
      </div>
   </div>

   <script>showDivs(slideIndex, '{{params.carouselId}}');</script>

{%- endmacro -%}


