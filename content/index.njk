---js
const layout = "layouts/homepage.njk";

const eleventyNavigation = {
	key: "Home",
	order: 1
};

const numberOfLatestPostsToShow = 3;
const latestNewsToShow = 4;

const title = 'Dorian H.';

---

<section class="top centre" id="Model">
	<h2>Companies I've worked with</h1>
	<!--TODO: FILL WITH INFINITE CAROUSEL OF LOGOS-->
</section>

<section class="centre" id="LatestProj">
	<h2>Latest Projects</h2>
	<!--TODO: RETRIEVE LATEST PROJECTS AND DISPLAY THEM-->
	<a class="btn primary" href="#WIP">View All Projects<i class="ri-arrow-right-s-fill"></i></a>
</section>

<section class="centre" id="FeaturedTest">
	<h2>Featured Testimonials</h2>
	<!--TODO RETRIEVE LIST OF TESTIMONIALS AND DISPLAY A FEW AT RANDOM-->
	{% for testimonial in testimonials %}
		<q>{{testimonial.testimonial}}</q>
		<p>{{testimonial.persona}}</p>
	{% endfor %}
	<div class="btn-row-inline">
		<a class="btn primary" href="#WIP">View All Projects<i class="ri-arrow-right-s-fill"></i></a>
		<a class="btn primary" href="#WIP">View All Projects<i class="ri-arrow-right-s-fill"></i></a>
	</div>
</section>

{# List every content page in the project #}
{#
<ul>
	{%- for entry in collections.all %}
	<li><a href="{{ entry.url }}"><code>{{ entry.url }}</code></a></li>
	{%- endfor %}
</ul>
#}
